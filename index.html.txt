<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>VOC Roadmap Control Center</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- React and ReactDOM CDNs -->
  <script src="https://unpkg.com/react@18/umd/react.production.min.js" crossorigin></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js" crossorigin></script>
  <!-- Babel CDN (Compiles JSX in the browser) -->
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body class="bg-slate-50">

  <!-- React Entry Point Container -->
  <div id="root"></div>

  <!-- Your Dashboard React Code -->
  <script type="text/babel">
    // 1. COPY & PASTE YOUR ENTIRE App.jsx CODE HERE 
    // (Ensure you replace any 'import' or 'export' lines at the very top/bottom, 
    // as the browser CDNs handle React imports globally!)
    
    const { useState, useMemo } = React;

    // ... [Paste the Icon component and App component code from App.jsx here] ...

    // 2. Render the App component into the root div
    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(<App />);
  </script>
</body>
</html>